// pos_buf是一个map, 每个value存储的应该是不同模型的所有点集
// ind_buf是一个map, 每个value表示对应pos_buf点集的关联(即产生边); value是3元素向量的vertor容器
// 每个3元素向量的每个元素(x, y, z)分别表示pos_buf点围成一个三角形
// 之所以这样做，是因为不同模型的点之间一般不发生关联，所以pos_buf用map存储多个键值对
// 之所以用ind_buf表示边，是因为一个点可能是多个三角形边的公共点，所以用ind_buf存储点在pos_buf中的索引，减少重复存储的内存  

// i是一个Vertor3i向量，分别存储组成三角形的三个点在pos_buf中的索引

// 将三角形三个点用齐次坐标表示

// Vector4f.head<3>()表示取4元素向量的前3个元素组成3元素向量

// 设置每个顶点不同颜色

